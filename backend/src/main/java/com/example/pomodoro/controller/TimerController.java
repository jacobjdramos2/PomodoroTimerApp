/*
 * Define REST endpoints, e.g., /start, /stop, /pause
 * Holds the REST controllers that handle incoming HTTP requests and send responses.
 */

public class TimerController {

    // startTimer(): Starts timer
    // pauseTimer(): Pauses the running timer
    // stopTimer(): Stops and resets the timer
    // getTimerStatus(): Return the current status of the timer (running, paused, stopped)

    // Use Spring's @RestController to define API endpoints and wire them to the service layer
    
}